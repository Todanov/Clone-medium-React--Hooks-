import React from 'react'
import s from './taglist.module.css'

const TagList = ({tags})=>{
    return(
        <ul className={s.ul}>
        {tags.map((tag,index)=>(
            <li key={index} className={s.li}>{tag}</li>
        ))}
    </ul>
    )
}

export default TagList